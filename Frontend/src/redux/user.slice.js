import { createSlice } from "@reduxjs/toolkit";

const userSlice = createSlice({
    name:"User",
    initialState:{
        UserData:[],
        loading:false
    },
    reducers:{},
  

 
});

export default userSlice.reducer;